# site-v2 — Shark-ISR VTOL portfolio site (superseded)

> **Superseded by `site-v3/`** (2026-07-13) — the current public single-pager. v2 is kept for
> its 3D sections; its interactive confidence-gate demo has been ported to v3
> (`site-v3/components/GateDemo.tsx`, constants in `site-v3/lib/guidance.ts`). No new work here.

Next.js 15 + Three.js/R3F engineering portfolio for the autonomous shark-monitoring VTOL project.
Showcases the autonomy architecture, hardware stack, perception pipeline, and SITL results.

## Dev

```bash
npm install
npm run dev        # http://localhost:3000
```

## Build

```bash
npm run build
npm start
```

See the [root README](../README.md) for full project context.
